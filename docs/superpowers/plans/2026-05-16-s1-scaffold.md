# S1: Scaffold + Visual Shell — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Standing Vite + React 18 + TS strict + Tailwind v4 app at `localhost:5173` with brand styling, a working address input, and an R3F canvas showing a rotating low-poly building. No real APIs.

**Architecture:** Single-page Vite + React SPA. Tailwind v4 with CSS-first `@theme` brand tokens. R3F scene isolated under `src/scene/` with separate files for `Canvas`, `Lighting`, and `PlaceholderBuilding`. Address input is a controlled component under `src/components/AddressInput/`. Submit handler logs only — no API wiring in S1.

**Tech Stack:** Vite 6, React 18, TypeScript (strict), Tailwind CSS v4 (`@tailwindcss/vite`), three + @react-three/fiber + @react-three/drei. Package manager: npm.

**Note on placeholder rotation:** The `useFrame` rotation on `PlaceholderBuilding` is **S1-only**. When real Google Solar roof geometry replaces the placeholder in S3, the building is static and the user orbits the camera. The rotation logic must not be carried forward into the real geometry component.

---

## File Structure

```
package.json                          # deps, scripts
package-lock.json
vite.config.ts                        # react + tailwind plugin
tsconfig.json                         # root tsconfig (references)
tsconfig.app.json                     # app TS config (strict)
tsconfig.node.json                    # node-side TS config (vite.config)
index.html                            # Google Fonts <link>, title, viewport
.gitignore                            # extend with node_modules/dist
src/main.tsx                          # React root
src/App.tsx                           # Branded layout: header, input col, scene col
src/index.css                         # @import "tailwindcss"; + @theme tokens
src/vite-env.d.ts                     # Vite types
src/components/AddressInput/
  index.tsx                           # Controlled input + submit button
src/scene/
  Canvas.tsx                          # <Canvas> setup, camera, OrbitControls
  Lighting.tsx                        # Ambient + directional light rig
  PlaceholderBuilding.tsx             # Low-poly flat-roof building, rotates (S1 only)
```

---

## Task 1: Scaffold base project files

**Files:**
- Create: `package.json`, `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`, `vite.config.ts`, `index.html`, `src/main.tsx`, `src/App.tsx`, `src/vite-env.d.ts`, `src/index.css`
- Modify: `.gitignore`

- [ ] **Step 1: Write package.json**

```json
{
  "name": "energy-owl",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit"
  }
}
```

- [ ] **Step 2: Write tsconfig.json (project references)**

```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
```

- [ ] **Step 3: Write tsconfig.app.json (strict, app code)**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true
  },
  "include": ["src"]
}
```

- [ ] **Step 4: Write tsconfig.node.json (vite.config.ts)**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2023"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["vite.config.ts"]
}
```

- [ ] **Step 5: Write vite.config.ts**

```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vite";

export default defineConfig({
  plugins: [react(), tailwindcss()],
});
```

- [ ] **Step 6: Write index.html (Google Fonts, branding)**

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Energy Owl</title>
    <meta
      name="description"
      content="Drop a US commercial address, get a 3D roof, solar production estimates, and an AI-generated narrative report."
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Instrument+Serif&family=DM+Sans:wght@400;500;700&family=JetBrains+Mono:wght@400;500&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 7: Write src/vite-env.d.ts**

```ts
/// <reference types="vite/client" />
```

- [ ] **Step 8: Write src/index.css (Tailwind v4 + brand @theme)**

```css
@import "tailwindcss";

@theme {
  --color-forest: #0F1A12;
  --color-sage: #6B8F71;
  --color-gold: #B8943E;
  --color-offwhite: #F5F2EB;

  --font-serif: "Instrument Serif", ui-serif, Georgia, serif;
  --font-sans: "DM Sans", ui-sans-serif, system-ui, sans-serif;
  --font-mono: "JetBrains Mono", ui-monospace, monospace;
}

html, body, #root {
  height: 100%;
}

body {
  margin: 0;
  background-color: var(--color-forest);
  color: var(--color-offwhite);
  font-family: var(--font-sans);
  -webkit-font-smoothing: antialiased;
}
```

- [ ] **Step 9: Write src/main.tsx**

```tsx
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import App from "./App";
import "./index.css";

createRoot(document.getElementById("root")!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

- [ ] **Step 10: Write minimal src/App.tsx (placeholder, refined in later task)**

```tsx
export default function App() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <h1 className="font-serif text-4xl text-offwhite">Energy Owl</h1>
    </main>
  );
}
```

- [ ] **Step 11: Extend .gitignore**

Append the following lines to `.gitignore`:

```
dist-ssr
*.local
.vite
*.log
.idea
.vscode/*
!.vscode/extensions.json
```

(Existing entries already cover `.env.local`, `node_modules`, `dist`, `.DS_Store`.)

---

## Task 2: Install dependencies and verify dev server

**Files:**
- Modify: `package.json` (npm adds deps)
- Create: `package-lock.json`, `node_modules/`

- [ ] **Step 1: Install runtime dependencies**

Run from project root:
```bash
npm install react@^18 react-dom@^18 three @react-three/fiber @react-three/drei
```

- [ ] **Step 2: Install dev dependencies**

```bash
npm install -D vite @vitejs/plugin-react typescript @types/react @types/react-dom @types/three tailwindcss @tailwindcss/vite
```

- [ ] **Step 3: Verify type check passes**

Run: `npm run typecheck`
Expected: exits 0 with no output.

- [ ] **Step 4: Verify dev server starts**

Run: `npm run dev` (background, then curl the URL)
Expected: server listening on `http://localhost:5173` and `GET /` returns HTML containing `<div id="root"></div>`.

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json tsconfig*.json vite.config.ts index.html .gitignore src/
git commit -m "S1: scaffold Vite + React 18 + TS strict + Tailwind v4 base"
```

---

## Task 3: Build branded App shell layout

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: Replace src/App.tsx with the branded shell**

```tsx
export default function App() {
  return (
    <div className="min-h-screen flex flex-col bg-forest text-offwhite">
      <header className="px-6 md:px-10 pt-8 pb-4 border-b border-sage/20">
        <h1 className="font-serif text-4xl md:text-5xl tracking-tight">
          Energy Owl
        </h1>
        <p className="mt-2 text-sm md:text-base text-offwhite/70 max-w-xl">
          Drop a US commercial address. Get a 3D roof, production estimates,
          and an AI-generated narrative report.
        </p>
      </header>

      <div className="flex-1 grid grid-cols-1 lg:grid-cols-[minmax(320px,420px)_1fr] gap-6 px-6 md:px-10 py-8">
        <section className="flex flex-col gap-6">
          <div className="rounded-lg border border-sage/20 bg-forest p-5">
            <p className="text-sm text-offwhite/60">
              Results panel — wired in S2.
            </p>
          </div>
        </section>

        <section className="rounded-lg border border-sage/20 bg-forest min-h-[420px]">
          <div className="h-full flex items-center justify-center text-offwhite/40 text-sm">
            3D scene — wired in next task.
          </div>
        </section>
      </div>

      <footer className="px-6 md:px-10 py-4 border-t border-sage/20 text-xs text-offwhite/40 font-mono">
        shiftatlas.tech · portfolio demo
      </footer>
    </div>
  );
}
```

- [ ] **Step 2: Verify visually**

Run: `npm run dev` (if not already running) and load `http://localhost:5173`.
Expected: Deep Forest background, Instrument Serif "Energy Owl" heading, DM Sans body, Sage hairline borders, mono footer text. No horizontal scroll at 375px width.

- [ ] **Step 3: Verify type check passes**

Run: `npm run typecheck`
Expected: exits 0.

---

## Task 4: Build AddressInput component

**Files:**
- Create: `src/components/AddressInput/index.tsx`
- Modify: `src/App.tsx`

- [ ] **Step 1: Write src/components/AddressInput/index.tsx**

```tsx
import { useState, type FormEvent } from "react";

type Props = {
  onSubmit: (address: string) => void;
};

export default function AddressInput({ onSubmit }: Props) {
  const [value, setValue] = useState("");

  function handleSubmit(e: FormEvent<HTMLFormElement>) {
    e.preventDefault();
    const trimmed = value.trim();
    if (!trimmed) return;
    onSubmit(trimmed);
  }

  return (
    <form onSubmit={handleSubmit} className="flex flex-col gap-3">
      <label htmlFor="address" className="text-sm text-offwhite/70">
        Commercial address
      </label>
      <input
        id="address"
        type="text"
        value={value}
        onChange={(e) => setValue(e.target.value)}
        placeholder="1 Apple Park Way, Cupertino, CA"
        autoComplete="off"
        spellCheck={false}
        className="
          w-full rounded-md
          bg-forest border border-sage/40
          px-3 py-2 text-base text-offwhite placeholder:text-offwhite/30
          focus:outline-none focus:border-sage focus:ring-2 focus:ring-sage/40
          transition
        "
      />
      <button
        type="submit"
        disabled={!value.trim()}
        className="
          self-start rounded-md
          bg-gold text-forest font-medium
          px-4 py-2 text-sm
          hover:bg-gold/90 active:bg-gold/80
          disabled:opacity-40 disabled:cursor-not-allowed
          transition
        "
      >
        Analyze
      </button>
    </form>
  );
}
```

- [ ] **Step 2: Wire AddressInput into App.tsx**

In `src/App.tsx`, replace the placeholder results panel with the input, and add a submit handler:

```tsx
import AddressInput from "./components/AddressInput";

export default function App() {
  function handleAddressSubmit(address: string) {
    // S1: log only. S2 will trigger API calls here.
    console.log("Address submitted:", address);
  }

  return (
    <div className="min-h-screen flex flex-col bg-forest text-offwhite">
      <header className="px-6 md:px-10 pt-8 pb-4 border-b border-sage/20">
        <h1 className="font-serif text-4xl md:text-5xl tracking-tight">
          Energy Owl
        </h1>
        <p className="mt-2 text-sm md:text-base text-offwhite/70 max-w-xl">
          Drop a US commercial address. Get a 3D roof, production estimates,
          and an AI-generated narrative report.
        </p>
      </header>

      <div className="flex-1 grid grid-cols-1 lg:grid-cols-[minmax(320px,420px)_1fr] gap-6 px-6 md:px-10 py-8">
        <section className="flex flex-col gap-6">
          <div className="rounded-lg border border-sage/20 bg-forest p-5">
            <AddressInput onSubmit={handleAddressSubmit} />
          </div>
          <div className="rounded-lg border border-sage/20 bg-forest p-5">
            <p className="text-sm text-offwhite/60">
              Results panel — wired in S2.
            </p>
          </div>
        </section>

        <section className="rounded-lg border border-sage/20 bg-forest min-h-[420px]">
          <div className="h-full flex items-center justify-center text-offwhite/40 text-sm">
            3D scene — wired in next task.
          </div>
        </section>
      </div>

      <footer className="px-6 md:px-10 py-4 border-t border-sage/20 text-xs text-offwhite/40 font-mono">
        shiftatlas.tech · portfolio demo
      </footer>
    </div>
  );
}
```

- [ ] **Step 3: Verify visually**

Reload `http://localhost:5173`. Type an address, click Analyze. Open browser devtools console; expect `"Address submitted: <text>"`. Submit button disabled when input is empty/whitespace.

---

## Task 5: Build R3F scene with rotating placeholder building

**Files:**
- Create: `src/scene/Lighting.tsx`, `src/scene/PlaceholderBuilding.tsx`, `src/scene/Canvas.tsx`
- Modify: `src/App.tsx`

- [ ] **Step 1: Write src/scene/Lighting.tsx**

```tsx
export default function Lighting() {
  return (
    <>
      <ambientLight intensity={0.4} color="#F5F2EB" />
      <directionalLight
        position={[8, 12, 6]}
        intensity={1.1}
        color="#FBE7B0"
        castShadow
      />
      <directionalLight
        position={[-6, 4, -4]}
        intensity={0.3}
        color="#6B8F71"
      />
    </>
  );
}
```

- [ ] **Step 2: Write src/scene/PlaceholderBuilding.tsx**

```tsx
import { useRef } from "react";
import { useFrame } from "@react-three/fiber";
import type { Group } from "three";

/**
 * S1-only placeholder. Removed in S3 when real Google Solar roof segments
 * replace it. The rotation here is for visual demo of the empty scene —
 * real geometry will be static and the user orbits the camera.
 */
export default function PlaceholderBuilding() {
  const ref = useRef<Group>(null);

  useFrame((_, delta) => {
    if (ref.current) {
      ref.current.rotation.y += delta * 0.15;
    }
  });

  return (
    <group ref={ref}>
      <mesh position={[0, 1.5, 0]} castShadow receiveShadow>
        <boxGeometry args={[6, 3, 4]} />
        <meshStandardMaterial color="#6B8F71" roughness={0.7} metalness={0.05} />
      </mesh>

      <mesh position={[-1.4, 3.25, 0.6]} castShadow>
        <boxGeometry args={[1.4, 0.5, 1]} />
        <meshStandardMaterial color="#4F6B53" roughness={0.8} />
      </mesh>
      <mesh position={[1.6, 3.15, -0.8]} castShadow>
        <boxGeometry args={[1, 0.3, 0.8]} />
        <meshStandardMaterial color="#4F6B53" roughness={0.8} />
      </mesh>

      <mesh position={[0, 0, 0]} rotation={[-Math.PI / 2, 0, 0]} receiveShadow>
        <planeGeometry args={[40, 40]} />
        <meshStandardMaterial color="#0F1A12" roughness={1} />
      </mesh>
    </group>
  );
}
```

- [ ] **Step 3: Write src/scene/Canvas.tsx**

```tsx
import { Canvas } from "@react-three/fiber";
import { OrbitControls } from "@react-three/drei";
import Lighting from "./Lighting";
import PlaceholderBuilding from "./PlaceholderBuilding";

export default function Scene() {
  return (
    <Canvas
      shadows
      camera={{ position: [10, 8, 12], fov: 35 }}
      dpr={[1, 2]}
      gl={{ antialias: true }}
      style={{ background: "transparent" }}
    >
      <Lighting />
      <PlaceholderBuilding />
      <OrbitControls
        enablePan={false}
        enableZoom
        minDistance={8}
        maxDistance={30}
        maxPolarAngle={Math.PI / 2.1}
      />
    </Canvas>
  );
}
```

- [ ] **Step 4: Wire Scene into App.tsx**

In `src/App.tsx`, import the scene and replace the placeholder text section:

```tsx
import AddressInput from "./components/AddressInput";
import Scene from "./scene/Canvas";

export default function App() {
  function handleAddressSubmit(address: string) {
    console.log("Address submitted:", address);
  }

  return (
    <div className="min-h-screen flex flex-col bg-forest text-offwhite">
      <header className="px-6 md:px-10 pt-8 pb-4 border-b border-sage/20">
        <h1 className="font-serif text-4xl md:text-5xl tracking-tight">
          Energy Owl
        </h1>
        <p className="mt-2 text-sm md:text-base text-offwhite/70 max-w-xl">
          Drop a US commercial address. Get a 3D roof, production estimates,
          and an AI-generated narrative report.
        </p>
      </header>

      <div className="flex-1 grid grid-cols-1 lg:grid-cols-[minmax(320px,420px)_1fr] gap-6 px-6 md:px-10 py-8">
        <section className="flex flex-col gap-6">
          <div className="rounded-lg border border-sage/20 bg-forest p-5">
            <AddressInput onSubmit={handleAddressSubmit} />
          </div>
          <div className="rounded-lg border border-sage/20 bg-forest p-5">
            <p className="text-sm text-offwhite/60">
              Results panel — wired in S2.
            </p>
          </div>
        </section>

        <section className="rounded-lg border border-sage/20 bg-forest min-h-[420px] overflow-hidden">
          <Scene />
        </section>
      </div>

      <footer className="px-6 md:px-10 py-4 border-t border-sage/20 text-xs text-offwhite/40 font-mono">
        shiftatlas.tech · portfolio demo
      </footer>
    </div>
  );
}
```

- [ ] **Step 5: Verify visually**

Reload `http://localhost:5173`. Expect:
- A sage-green box building with two darker rooftop boxes (HVAC stand-ins) rotating slowly on the Y axis.
- Soft warm directional light from the upper-right.
- Mouse drag rotates the camera (orbit), wheel zooms within bounds.
- No console errors.

- [ ] **Step 6: Verify type check**

Run: `npm run typecheck`
Expected: exits 0.

---

## Task 6: Final integration check + commit

- [ ] **Step 1: Mobile responsiveness check**

In browser devtools, switch to 375px viewport. Confirm:
- No horizontal scroll.
- Header text legible.
- Address input full width of its column.
- 3D scene visible (may be shorter).
- Layout stacks single-column.

- [ ] **Step 2: Build to confirm production compile**

Run: `npm run build`
Expected: exits 0, produces `dist/` with `index.html` and bundled assets.

- [ ] **Step 3: Final commit**

```bash
git add -A
git commit -m "S1: address input + R3F scene with rotating placeholder building"
```

- [ ] **Step 4: S1 done — sanity checklist**

- [ ] `npm run dev` starts cleanly.
- [ ] Brand colors visible on page (`#0F1A12`, `#F5F2EB`, `#6B8F71`, `#B8943E`).
- [ ] Instrument Serif on heading, DM Sans on body, JetBrains Mono on footer.
- [ ] Address input + submit + console log works.
- [ ] R3F building rotates and is orbitable.
- [ ] `npm run typecheck` and `npm run build` both exit 0.
- [ ] No real API code present yet (S2 territory).

---

## What's deliberately NOT in S1

- No Tanstack Query (S2 when APIs are wired).
- No real API clients in `src/lib/apis/` (S2).
- No tests (visual scaffolding; Vitest added in S2 alongside the first API client).
- No backend / serverless function (parked for v2 entirely).
- No address geocoding (S2: NREL takes lat/lon — we'll figure out the geocode path during S2 planning).
- No results panel logic — just a placeholder block.
